[formatter.nix]
command = "nixpkgs-fmt"
includes = ["*.nix"]

[formatter.markdown]
command = "mdformat"
includes = [
         "*.md",
]

[formatter.shell]
command = "shfmt"
options = [
        "-s",
        "-w",
        "-i",
        "2",
]
includes = ["*.sh"]
